<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || "POS System" %> | POS System</title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <% const shellLayout = typeof showShell === "undefined" ? true : showShell; %>
    <% if (shellLayout) { %>
      <div class="container">
    <% } %>
